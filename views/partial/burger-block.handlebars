<li>
	{{name}}

	<button class="change-state" data-id="{{id}}" data-newsleep="{{devoured}}">
		{{#if devoured}}YUMMY{{else}}TIME TO EAT!!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>